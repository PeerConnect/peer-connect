// imageFind();

function imageFind() {
  const images = document.getElementsByTagName('img');
  // const imageArray = [];
  // Object.values(images)
  // for (let i = 0; i < images.length; i += 1) {
  //   //give specific data-id to each img tag
  // }
  // console.log(images)
  return images;
}
